# Fix nginx types_hash warning by increasing hash table size
# This warning occurs when nginx can't build an optimal hash table for MIME types
# Increasing these values eliminates the warning and improves performance
types_hash_max_size 2048;
types_hash_bucket_size 128;

